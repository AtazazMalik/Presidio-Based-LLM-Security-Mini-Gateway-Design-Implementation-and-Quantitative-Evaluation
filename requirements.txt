# Core Presidio libraries
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354

# NLP backend (spaCy)
# A language model is required for PERSON (NER) detection.
# If no model is installed, the gateway falls back to spacy.blank('en')
# which still supports all regex/pattern-based recognizers.
#
# To install the recommended model (requires internet access):
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.7.1/en_core_web_lg-3.7.1-py3-none-any.whl
#
# Or use the smaller model:
#   python -m spacy download en_core_web_sm
spacy>=3.7.0,<3.8.0
